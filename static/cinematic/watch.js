let version = null, candidate = null;let idle = 0;addEventListener('keydown',()=>{idle=Date.now()});addEventListener('pointerdown',()=>{idle=Date.now()});setInterval(async()=>{if(document.hidden)return;try{const response=await fetch('/__version',{cache:'no-store'});if(!response.ok)return;const data=await response.json();if(!version){version=data.version;return}if(data.version!==version){if(candidate===data.version&&Date.now()-idle>2500&&!['INPUT','TEXTAREA'].includes(document.activeElement?.tagName)){location.reload()}candidate=data.version}else{candidate=null}}catch{}},1800);